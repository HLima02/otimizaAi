import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  centerContent: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  percent: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  currentEarning: {
    fontSize: 28,
    fontWeight: 'bold'
  },
  currentGoal: {
    fontSize: 14,
    color: '#777',
  },
})